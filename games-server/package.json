{
  "name": "ollij-games-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Realtime server for ollij.fi games — one process, a Socket.IO namespace per game.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.0",
    "express": "^4.21.0",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  },
  "engines": { "node": ">=18" }
}
