_site/*
compile_llms.py
update_llms.sh
setup_auto_update.py

# Node dependencies (factory-market + aumann servers)
node_modules/

# Translation harness: working material + API key, not for the public repo
translation/
# GA editor — personal corpus + runtime data. NEVER publish on the public site
# (captures, append-only log, draft history, telemetry, dispatched tasks).
# node_modules/ is already ignored globally above.
_ga/

# private quick-capture notes — a real post locally, but never committed to the public repo
_writing/saved-thoughts.md
# the canonical TODO list — same treatment
_writing/todo.md
assets/uploads/sr-img/
# design mockups render unpublished draft content — local-only, never on the public site
dashboard/design/

# protected review pages (see _protect/README.md): the registry maps slugs to
# tokens AND passphrases — strictly local. The tool itself is committed.
_protect/registry.json
